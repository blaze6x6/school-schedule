DB_PASSWORD=SchoolSchedule2025!

# JWT Secret (change this in production!)
JWT_SECRET=school-schedule-jwt-secret-change-me-in-production

# MariaDB root password (only if using MariaDB)
DB_ROOT_PASSWORD=rootpassword
